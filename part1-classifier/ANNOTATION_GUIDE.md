# 📝 Руководство по разметке примеров

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install anthropic pandas openpyxl scikit-learn python-dotenv
```

### 2. Настройка API ключа

Создайте файл `.env` в корне проекта:

```bash
ANTHROPIC_API_KEY=your-api-key-here
```

Или установите переменную окружения:

```bash
# Windows
set ANTHROPIC_API_KEY=your-key

# Linux/Mac
export ANTHROPIC_API_KEY=your-key
```

### 3. Размещение данных

Положите Excel файл `31.xlsx` в корень проекта (`prompt-engineer-test/31.xlsx`)

### 4. Запуск разметки

```bash
cd part1-classifier
python annotate_examples.py
```

---

## Что происходит при запуске

1. **Загрузка данных** из Excel файла
2. **Обработка примеров** 1-45 через Claude Sonnet 4.5
3. **Сохранение результатов**:
   - `results.json` - полные результаты с метриками
   - `results_table.md` - таблица для сдачи

---

## Формат результатов

### results.json

```json
{
  "model": "claude-sonnet-4-5-20250929",
  "total_examples": 45,
  "metrics": {
    "accuracy": 0.85,
    "precision": 0.87,
    "recall": 0.82,
    "f1_score": 0.84,
    "confusion_matrix": {
      "true_positive": 14,
      "true_negative": 24,
      "false_positive": 2,
      "false_negative": 3
    }
  },
  "results": [
    {
      "id": 1,
      "assessment": 0,
      "reasoning": "ИИ задает наводящий вопрос...",
      "error_type": null
    }
  ]
}
```

### results_table.md

| ID | Оценка | Краткое пояснение | Тип ошибки |
|----|--------|-------------------|------------|
| 1  | 0      | ИИ задает наводящий вопрос... | - |
| 2  | 1      | ИИ неверно вычислил производную... | формула |

---

## Стоимость разметки

При использовании Claude Sonnet 4.5:

- **Вход**: ~500 tokens на пример × 45 = 22,500 tokens
- **Выход**: ~100 tokens на пример × 45 = 4,500 tokens
- **Стоимость**: ~$0.10 - $0.15 за всю разметку

---

## Ожидаемое время

- **Подготовка**: 1-2 минуты
- **Разметка 45 примеров**: 5-10 минут
- **Итого**: ~10-12 минут

---

## Возможные ошибки

### "ANTHROPIC_API_KEY not found"

**Решение**: Установите API ключ в .env файл или переменную окружения

### "File not found: ../31.xlsx"

**Решение**: Положите Excel файл в корень проекта

### "RateLimitError"

**Решение**: Claude API имеет лимиты запросов. Скрипт автоматически делает паузы, но если ошибка повторяется - подождите минуту и запустите снова.

---

## Проверка результатов

После выполнения разметки:

1. Откройте `results_table.md` - готовая таблица для сдачи
2. Проверьте `results.json` - полные результаты с метриками
3. Метрики должны быть:
   - Accuracy > 80%
   - Precision > 85%
   - Recall > 75%
   - F1-Score > 80%

---

## Ручная проверка

Если хотите проверить конкретный пример вручную:

```python
from classifier import MathErrorClassifier

classifier = MathErrorClassifier()

result = classifier.classify(
    task_text="Ваша задача",
    dialogue_history="История диалога",
    ai_response="Ответ ИИ"
)

print(result)
```

---

**Готово!** После разметки отправьте файл `results_table.md` работодателю.
