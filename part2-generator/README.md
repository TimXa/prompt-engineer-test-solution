# Часть 2: Генератор задач ЕГЭ №5 (Теория вероятностей)

## Описание

Веб-приложение для алгоритмической генерации задач ЕГЭ по математике (базовый уровень), задание №5 — теория вероятностей. Генерация задач происходит **полностью алгоритмически**, без использования нейросетей в рантайме.

## Выбор задания №5

**Почему именно задание №5 (теория вероятностей)?**

1. **Алгоритмичность**: Задачи на классическую вероятность имеют четкую математическую структуру, легко поддающуюся алгоритмической генерации
2. **Разнообразие контекстов**: Можно создавать множество разных сюжетов (билеты, каналы, лотереи и т.д.)
3. **Понятная верификация**: Легко проверить правильность сгенерированного ответа
4. **Актуальность**: Одно из базовых заданий ЕГЭ-2026
5. **Педагогическая ценность**: Хорошо подходит для тренировки концепции вероятности

## Структура проекта

```
part2-generator/
├── app.py                 # Flask приложение (API endpoints)
├── src/
│   └── generator.py       # Алгоритмический генератор задач
├── templates/
│   └── index.html        # Главная страница (UI)
├── static/
│   ├── css/
│   │   └── style.css     # Стили (Bootstrap 5 + custom)
│   └── js/
│       └── app.js        # Frontend логика
├── requirements.txt       # Python зависимости
└── README.md             # Этот файл
```

## Возможности

### 5 типов задач

1. **Экзаменационные билеты** - Ученик тянет билет на экзамене
2. **Телевизионные каналы** - Случайное переключение каналов
3. **Лотереи и розыгрыши** - Покупка лотерейного билета
4. **Выбор объектов** - Карты, дни в году, детали в партии
5. **Цветные шары** - Классическая задача с шарами в коробке

### 3 уровня сложности

- **Лёгкая**: Малые числа (10-25), простые дроби
- **Средняя**: Средние числа (30-100), разнообразные дроби
- **Сложная**: Большие числа (100-1000+), сложные вычисления

### Для каждой задачи генерируется

- ✅ **Условие** - понятная формулировка с контекстом
- ✅ **Правильный ответ** - в виде дроби и десятичного числа
- ✅ **Пошаговое решение** - подробное объяснение

## Установка и запуск

### Локальный запуск

```bash
# 1. Установите зависимости
pip install -r requirements.txt

# 2. Запустите приложение
python app.py

# 3. Откройте в браузере
# http://localhost:5000
```

### Production запуск (с Gunicorn)

```bash
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

### Docker (опционально)

```bash
# Создайте Dockerfile
docker build -t ege-generator .
docker run -p 5000:5000 ege-generator
```

## Использование

### Веб-интерфейс

1. Откройте приложение в браузере
2. Выберите настройки:
   - Количество задач (1-20)
   - Сложность (лёгкая/средняя/сложная)
   - Тип задачи (или случайный микс)
3. Нажмите "Сгенерировать задачи"
4. Просматривайте задачи с решениями
5. Используйте кнопку печати для экспорта

### API

#### Генерация задач

```http
POST /api/generate
Content-Type: application/json

{
  "count": 5,
  "difficulty": "medium",
  "task_type": "exam_tickets"
}
```

**Ответ:**
```json
{
  "success": true,
  "count": 5,
  "tasks": [
    {
      "id": 1,
      "condition": "На экзамене 30 вопросов...",
      "answer": 0.8,
      "answer_fraction": "4/5",
      "solution": "Шаг 1: ...",
      "type": "exam_tickets",
      "difficulty": "medium"
    }
  ]
}
```

#### Список типов задач

```http
GET /api/task-types
```

## Технологии

### Backend
- **Python 3.10+**
- **Flask 3.0** - веб-фреймворк
- **Fractions** - для работы с дробями

### Frontend
- **Bootstrap 5.3** - UI фреймворк
- **Bootstrap Icons** - иконки
- **Vanilla JavaScript** - без зависимостей
- **Google Fonts (Inter)** - типографика

### Особенности
- 🎨 Современный градиентный дизайн
- 📱 Адаптивная верстка (mobile-first)
- ⚡ Плавные анимации и transitions
- 🖨️ Поддержка печати
- 🎯 REST API для интеграций

## Примеры сгенерированных задач

### Пример 1: Экзаменационные билеты
```
На экзамене 40 вопросов. Андрей не выучил 8 из них.
Найдите вероятность того, что ему достанется выученный вопрос.

Ответ: 4/5 = 0.8000

Решение:
Шаг 1: Определяем общее количество возможных исходов
Всего вопросов: 40

Шаг 2: Определяем количество благоприятных исходов
Выученных вопросов: 40 - 8 = 32

Шаг 3: Вычисляем вероятность по формуле P = m/n
P = 32/40 = 4/5 = 0.8000
```

### Пример 2: Телеканалы
```
Маша включает телевизор на случайном канале.
Всего телевизор показывает 45 каналов, из них 9 показывают новости.
Найдите вероятность того, что телевизор окажется настроен на канал,
НЕ показывающий новости.

Ответ: 4/5 = 0.8000
```

## Алгоритм генерации

### Как это работает

1. **Выбор типа задачи** - случайно или по запросу пользователя
2. **Генерация параметров** - случайные числа в зависимости от сложности
3. **Формирование условия** - подстановка в шаблон с вариациями
4. **Вычисление ответа** - точное вычисление с использованием Fraction
5. **Создание решения** - пошаговое объяснение

### Пример кода

```python
def _generate_exam_tickets(difficulty='medium'):
    total_tickets = random.choice([20, 24, 25, 30, 40])
    unstudied = random.randint(2, min(6, total_tickets - 2))
    studied = total_tickets - unstudied

    condition = f"На экзамене {total_tickets} вопросов..."
    fraction = Fraction(studied, total_tickets)
    answer = float(fraction)

    return {
        "condition": condition,
        "answer": answer,
        "answer_fraction": f"{fraction}",
        "solution": "..."
    }
```

## Расширение функционала

### Как добавить новый тип задачи

1. Создайте метод генератора в `generator.py`:
```python
def _generate_new_type(self, difficulty='medium'):
    # Ваша логика генерации
    return {
        "condition": "...",
        "answer": 0.5,
        "answer_fraction": "1/2",
        "solution": "..."
    }
```

2. Добавьте в словарь `task_templates`:
```python
self.task_templates = {
    ...
    "new_type": self._generate_new_type
}
```

3. Обновите UI (опционально)

## Ограничения

- Генерируются только задачи на **классическую вероятность** (формула P = m/n)
- Не поддерживаются комбинаторные задачи высокой сложности
- Ограничение: максимум 20 задач за один запрос

## Дальнейшее развитие

### Возможные улучшения

1. **Больше типов задач**:
   - Задачи с монетами
   - Задачи с костями
   - Задачи с картами (более сложные)
   - Геометрические вероятности

2. **Расширенная функциональность**:
   - Экспорт в PDF
   - Сохранение истории генерации
   - Создание тестов с ответами
   - Статистика сложности

3. **Интеграции**:
   - API для образовательных платформ
   - Telegram бот
   - Интеграция с LMS системами

4. **Улучшение UI/UX**:
   - Темная тема
   - Больше анимаций
   - Интерактивные графики вероятностей
   - Прогресс-бар для пакетной генерации

## Лицензия

MIT License - свободное использование в учебных целях

---

**Автор**: TimXa
**Дата**: Февраль 2026
**Версия**: 1.0
**Контакт**: [GitHub](https://github.com/timxa)
